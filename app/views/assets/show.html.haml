- title "Summary of #{@asset.name} Holdings"

%h2 Position Summary

%ul#position
  %li#amount_paid= "Position purchase value = #{number_to_currency(@asset.amount_paid, :unit => '£')}"
      
%h2 Transactions

%table#transactions
  %tr
    %th Date
    %th Units
    %th Value
    %th Costs
  - for transaction in @asset.transactions
    %tr.transaction
      %td= transaction.date.strftime("%d %B %Y")
      %td= transaction.units
      %td= number_to_currency(transaction.total_value, :unit => '£')
      %td= number_to_currency(transaction.cost, :unit => '£')

%p
  = link_to "View All", assets_path