- title "Purchasing transactions for #{@position.asset.name}"

= render :partial => 'transactions/index'

%p= link_to "New Purchase", new_position_purchase_path(@position)
                                                                            